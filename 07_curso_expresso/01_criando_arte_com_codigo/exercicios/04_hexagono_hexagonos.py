import turtle

hexagono = turtle.Turtle()
hexagono.hideturtle()

# Seu código a partir daqui
angulo = 60
lado = 100
desconto = 60


hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(desconto)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(desconto)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(desconto)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(desconto)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(desconto)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(angulo)
hexagono.forward(lado)

hexagono.left(desconto)

# Instrução necessária para que a janela não se feche
turtle.mainloop()